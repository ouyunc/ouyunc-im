# 资源监控使用指南

## 概述

`ResourceMonitor` 提供了统一的线程池和 Caffeine 缓存监控功能，支持：
- 线程池指标收集（活跃线程、池大小、队列大小、任务统计等）
- Caffeine 缓存指标收集（命中率、大小、加载次数、淘汰次数等）
- 定期监控报告输出
- 健康检查功能

## 功能特性

### 1. 线程池监控

自动监控所有通过 `ThreadPoolManager` 管理的线程池，包括：
- 活跃线程数
- 池大小
- 已完成任务数
- 总任务数
- 队列大小
- 运行状态

### 2. 缓存监控

支持监控所有已注册的 Caffeine 缓存，包括：
- 缓存大小
- 命中率
- 命中次数
- 未命中次数
- 加载次数
- 淘汰次数
- 加载耗时

### 3. 健康检查

自动检查：
- 线程池是否关闭或终止
- 线程池队列是否积压
- 缓存命中率是否过低

## 使用方法

### 1. 初始化监控

在服务启动时，监控会自动初始化。在 `StandardMessageServer.afterPropertiesSet()` 中会调用：

```java
MonitorInitializer.initialize();
```

这会注册所有需要监控的缓存实例。

### 2. 启动定期监控

在服务启动成功后（`ServerStartupEventListener`），会自动启动定期监控：

```java
MonitorInitializer.startMonitoring();  // 默认每5分钟输出一次
```

或自定义监控周期：

```java
MonitorInitializer.startMonitoring(10);  // 每10分钟输出一次
```

### 3. 手动查询指标

#### 获取线程池指标

```java
Map<ThreadPoolId, ThreadPoolMetrics> poolMetrics = ResourceMonitor.getThreadPoolMetrics();
poolMetrics.forEach((id, metrics) -> {
    System.out.println("线程池: " + id);
    System.out.println("  活跃线程: " + metrics.activeThreads());
    System.out.println("  池大小: " + metrics.poolSize());
    System.out.println("  队列大小: " + metrics.queueSize());
});
```

#### 获取缓存指标

```java
Map<String, CacheMetrics> cacheMetrics = ResourceMonitor.getCacheMetrics();
cacheMetrics.forEach((name, metrics) -> {
    System.out.println("缓存: " + name);
    System.out.println("  大小: " + metrics.size());
    System.out.println("  命中率: " + String.format("%.2f%%", metrics.hitRate() * 100));
    System.out.println("  命中次数: " + metrics.hitCount());
    System.out.println("  未命中次数: " + metrics.missCount());
});
```

#### 获取所有指标

```java
ResourceMetricsSnapshot snapshot = ResourceMonitor.getAllMetrics();
System.out.println("线程池数量: " + snapshot.threadPools().size());
System.out.println("缓存数量: " + snapshot.caches().size());
```

### 4. 手动输出监控报告

```java
ResourceMonitor.logAllMetrics();  // 输出所有指标到日志
ResourceMonitor.logThreadPoolMetrics();  // 仅输出线程池指标
ResourceMonitor.logCacheMetrics();  // 仅输出缓存指标
```

### 5. 健康检查

```java
HealthCheckResult result = ResourceMonitor.checkHealth();
if (!result.isHealthy()) {
    System.out.println("健康检查发现问题:");
    result.getIssues().forEach(System.out::println);
    result.getWarnings().forEach(System.out::println);
}
```

### 6. 注册自定义缓存

如果需要监控自定义的 Caffeine 缓存：

```java
// 方式1：直接注册 LoadingCache
LoadingCache<String, Object> myCache = Caffeine.newBuilder()
    .recordStats()  // 必须启用统计
    .build(key -> loadValue(key));
ResourceMonitor.registerCache("myCache", myCache);

// 方式2：注册 CaffeineLocalCache 实例
CaffeineLocalCache<String, Object> localCache = new CaffeineLocalCache<>(
    "myCache", 
    Caffeine.newBuilder().recordStats().build(key -> loadValue(key))
);
ResourceMonitor.registerCache(localCache);
```

**注意**：要获取缓存统计信息，必须在创建 Caffeine 缓存时调用 `.recordStats()`。

## 监控报告示例

定期监控会输出如下格式的报告：

```
========== 资源监控报告 ==========
【线程池监控】
  message-send: 活跃线程=2, 池大小=4, 已完成任务=1234, 总任务=1236, 队列大小=0, 状态=运行中
  message-processor: 活跃线程=5, 池大小=8, 已完成任务=5678, 总任务=5683, 队列大小=0, 状态=运行中
  router: 活跃线程=1, 池大小=2, 已完成任务=890, 总任务=891, 队列大小=0, 状态=运行中
【缓存监控】
  localClientInfoCache: 大小=150, 命中率=95.23%, 命中=2000, 未命中=100, 加载=100, 淘汰=5, 加载耗时=12.34ms
  messageProcessorCache: 大小=20, 命中率=99.50%, 命中=1990, 未命中=10, 加载=10, 淘汰=0, 加载耗时=0.50ms
  friendEntity: 大小=5000, 命中率=88.45%, 命中=8845, 未命中=1155, 加载=1155, 淘汰=200, 加载耗时=45.67ms
==================================
```

## 配置说明

### 启用缓存统计

要监控 Caffeine 缓存，必须在创建缓存时启用统计：

```java
Caffeine.newBuilder()
    .recordStats()  // 必须添加此行
    .maximumSize(1000)
    .build();
```

### 监控周期配置

默认监控周期为 5 分钟，可以在启动时自定义：

```java
// 在 ServerStartupEventListener 中
MonitorInitializer.startMonitoring(10);  // 每10分钟
```

## 注意事项

1. **性能影响**：启用 `recordStats()` 会有轻微的性能开销，但通常可以忽略不计
2. **内存占用**：统计信息会占用少量内存
3. **监控时机**：建议在服务启动完成后再启动监控，避免初始化阶段的干扰
4. **缓存注册**：只有已注册的缓存才会被监控，未注册的缓存不会出现在报告中

## 故障排查

### 缓存指标为空

- 检查是否调用了 `.recordStats()` 创建缓存
- 检查缓存是否已注册到 `ResourceMonitor`
- 检查缓存是否有访问（无访问时某些指标可能为 0）

### 线程池指标为 -1

- 某些线程池类型（如虚拟线程池）不支持部分指标
- 这是正常现象，不影响监控功能

### 监控报告未输出

- 检查是否调用了 `startMonitoring()`
- 检查日志级别是否允许 INFO 级别输出
- 检查是否有异常日志



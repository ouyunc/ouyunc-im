# 线程池集中管理使用指南

## 1. 已完成的迁移内容

- 引入 `com.ouyunc.base.executor.ThreadPoolManager`、`ThreadPoolConfig`、`ThreadPoolType`
- 所有核心模块（消息发送、消息处理、QoS、路由、仓储、事件分发、心跳、Redis 订阅、系统时钟）统一改为通过 `ThreadPoolManager` 获取执行器
- `StandardMessageServer.afterPropertiesSet()` 自动加载 `ouyunc-server.yml` 中的线程池配置并初始化
- JVM 关闭钩子和手动停服务时统一调用 `ThreadPoolManager.shutdownAll()`，确保资源释放
- `ThreadPoolManager.metrics()` 支持运行时指标采集

## 2. 如何扩展/调整线程池

### 2.1 配置线程池参数

在 `ouyunc-server.yml` 下追加或修改：

```yaml
ouyunc:
  message:
    thread-pool:
      repository:
        type: VIRTUAL        # 可选: VIRTUAL/FIXED/CACHED/SCHEDULED/SINGLE
        core-size: 8
        max-size: 16
        queue-capacity: 1000
        keep-alive-seconds: 60
        thread-name-prefix: repository
        daemon: true
        allow-core-timeout: false
```

> 亦可通过 JVM 参数或环境变量覆盖，例如：  
> `-Douyunc.message.thread-pool.repository.core-size=12`  
> `OUYUNC_MESSAGE_THREAD_POOL_REPOSITORY_CORE_SIZE=12`

### 2.2 新增业务线程池

1. 在 `ThreadPoolId` 中新增标识
2. 在 `ThreadPoolConfig.defaultConfig()` 中补充默认值
3. 在 `ThreadPoolManager` 中添加访问器（如 `public static ExecutorService xxxExecutor()`）
4. 在业务代码中改用新访问器
5. 更新 `ouyunc-server.yml` 提供配置项

## 3. 调用示例

```java
ThreadPoolManager.messageSendExecutor().execute(task);
ThreadPoolManager.qosTaskExecutor().submit(() -> {...});
ThreadPoolManager.clientHeartbeatScheduler()
        .scheduleWithFixedDelay(runnable, 0, 5, TimeUnit.SECONDS);
```

## 4. 验证清单

- [x] `ouyunc-server.yml` 已包含 `thread-pool` 配置块
- [x] 启动日志打印 `线程池配置初始化完成`
- [x] 关键模块（消息发送、路由、仓储、心跳、系统时钟、Redis 订阅）均不再直接调用 `Executors.new*`
- [x] `ThreadPoolManager.metrics()` 输出包含所有线程池
- [x] 优雅停机时 `ThreadPoolManager.shutdownAll()` 被调用

## 5. 监控与排查

- 调用 `ThreadPoolManager.logMetrics()` 可快速在日志中查看运行指标
- 如需可视化，可将 `metrics()` 返回的数据接入 Prometheus 或自建统计任务
- 建议在压测和生产环境分别调整 `core-size`、`queue-capacity` 等参数

## 6. 回滚策略

- 若需回退，可将 `ThreadPoolManager.initialise()` 调整回默认配置，或还原旧代码
- 保留旧的 `Executors` 实现分支可通过 Git 历史快速恢复

## 7. 后续建议

1. 纳入 Prometheus / Micrometer，建立统一监控看板
2. 基于运行指标评估各线程池是否需从虚拟线程切换到平台线程
3. 若有动态调参需求，可考虑在 `ThreadPoolManager` 暴露刷新入口
4. 对大流量场景按环境维护推荐参数模板，纳入文档


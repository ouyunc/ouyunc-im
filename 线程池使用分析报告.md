# 线程池使用分析报告（最新架构）

## 1. 架构概览

- **统一入口**：`com.ouyunc.base.executor.ThreadPoolManager`
- **配置来源**：`ouyunc-server.yml`（`ouyunc.message.thread-pool` 节点），可通过系统属性/环境变量覆盖（键前缀 `ouyunc.message.thread-pool.`）
- **覆盖范围**：
  - 消息发送、消息处理、QoS、路由、仓储、事件广播
  - 集群与外部客户端心跳
  - 系统时钟、Redis 订阅监听
  - 统一关闭钩子与运行时指标

| 线程池 | 对应 ID | 默认类型 | 默认线程名前缀 | 主要使用方 |
| --- | --- | --- | --- | --- |
| 消息发送 | `message-send` | VIRTUAL | `message-send` | `MessageHelper` |
| 消息处理 | `message-processor` | VIRTUAL | `message-processor` | `AbstractMessageProcessor` |
| QoS 重试 | `qos-task` | VIRTUAL | `qos-task` | `TimerTaskWrapper` |
| 路由 | `router` | VIRTUAL | `router` | `AbstractMessageRouter` |
| 仓储 | `repository` | VIRTUAL | `repository` | `DefaultRepository` |
| 事件广播 | `event-listener` | VIRTUAL | `event-listener` | `StandardMessageServer` |
| 客户端心跳 | `client-heartbeat` | SCHEDULED(核心 4) | `client-heartbeat` | 客户端维持心跳 |
| 集群心跳 | `cluster-client-heartbeat` | SCHEDULED(核心 1) | `cluster-client-heartbeat` | 集群内置客户端 |
| 系统时钟 | `system-clock` | SCHEDULED(核心 1) | `system-clock` | `SystemClock` |
| Redis 订阅 | `redis-pubsub` | SINGLE | `redis-pubsub` | `ServerStartupEventListener` |

> Netty 的 `EventLoopGroup` 依旧由 Netty 自行管理，不纳入该管理器。

## 2. 配置方式

`ouyunc-server.yml` 中新增段落：

```yaml
ouyunc:
  message:
    thread-pool:
      message-send:
        type: VIRTUAL
        thread-name-prefix: message-send
      client-heartbeat:
        type: SCHEDULED
        core-size: 4
        thread-name-prefix: client-heartbeat
      # 其余线程池同理，可按需覆盖
```

- 支持的字段：`type`、`core-size`、`max-size`、`queue-capacity`、`keep-alive-seconds`、`thread-name-prefix`、`daemon`、`allow-core-timeout`
- 运行期覆盖：通过 `-Douyunc.message.thread-pool.message-send.core-size=8` 或环境变量 `OUYUNC_MESSAGE_THREAD_POOL_MESSAGE_SEND_CORE_SIZE=8`
- `ThreadPoolManager.initialise(ThreadPoolConfig)` 在 `StandardMessageServer.afterPropertiesSet()` 内触发，可在自定义启动流程中重用

## 3. 运行时管理能力

- **监控**：`ThreadPoolManager.metrics()` 返回所有线程池指标（活跃线程数、池大小、任务数、队列长度等），`ThreadPoolManager.logMetrics()` 可直接输出日志
- **关闭策略**：
  - `ThreadPoolManager.shutdownAll()` 提供显式关闭
  - JVM 关闭钩子会自动调用，确保资源释放
  - 服务停机 (`AbstractMessageServer.stop()` / 关闭钩子) 中已统一调用

## 4. 迁移结果

- ✅ 所有核心线程池调用统一入口，无直接 `Executors.new*` 调用
- ✅ `SystemClock`、`TimerTaskWrapper` 等长期运行组件改用共享调度器，避免重复创建线程
- ✅ 数据仓储、消息发送、路由、事件处理等关键链路具备统一命名和监控能力
- ✅ YAML 配置 + 系统属性双通道支持环境差异化
- ✅ 服务关闭时统一释放，避免遗留守护线程

仍保持独立的线程池仅限开发/测试工具（例如 `ouyunc-client` 模块下的压测脚本），避免干扰主服务生命周期。

## 5. 后续建议

- 将监控指标接入 Prometheus / Micrometer，形成 Dashboard 与告警
- 结合业务负载，针对 `core-size`、`queue-capacity` 等参数在不同环境给出推荐模板
- 依赖统计数据，评估哪些场景需要从虚拟线程调整为平台线程
- 若需要动态调参，可在 `ThreadPoolManager` 中增加热更新接口

当前集中化方案已满足：
1. **统一管理与命名**
2. **配置可调、环境可覆盖**
3. **运行时监控与优雅关闭**

可作为后续性能调优与观测的基础设施。

